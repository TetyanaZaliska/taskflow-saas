
model User {
  id         Int @id @default(autoincrement())
  email      String @unique
  password   String?

  teams      TeamMember[]
  tasks      Task[]        // optional: assigned tasks
  activities Activity[]
  settings   Settings?

  createdAt  DateTime @default(now())
}

model Team {
  id        Int @id @default(autoincrement())
  name      String

  members   TeamMember[]
  projects  Project[]

  createdAt DateTime @default(now())
}

model TeamMember {
  id     Int @id @default(autoincrement())

  userId Int
  teamId Int

  user   User @relation(fields: [userId], references: [id])
  team   Team @relation(fields: [teamId], references: [id])

  role   TeamRole @default(MEMBER)

  createdAt DateTime @default(now())
}

enum TeamRole {
  ADMIN
  MEMBER
}

model Project {
  id          Int @id @default(autoincrement())
  name        String
  description String?

  teamId      Int
  team        Team @relation(fields: [teamId], references: [id])

  tasks       Task[]

  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model Task {
  id          Int @id @default(autoincrement())
  title       String
  description String?

  status      TaskStatus @default(TODO)
  priority    TaskPriority @default(MEDIUM)

  projectId   Int
  project     Project @relation(fields: [projectId], references: [id])

  assigneeId  Int?
  assignee    User? @relation(fields: [assigneeId], references: [id])

  createdById Int
  createdBy   User @relation(fields: [createdById], references: [id])

  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

enum TaskStatus {
  TODO
  IN_PROGRESS
  DONE
}

enum TaskPriority {
  LOW
  MEDIUM
  HIGH
}

model Settings {
  id        Int @id @default(autoincrement())

  userId    Int @unique
  user      User @relation(fields: [userId], references: [id])

  theme     String @default("system")
  language  String @default("en")

  notifications Boolean @default(true)

  updatedAt DateTime @updatedAt
}

model Activity {
  id        Int @id @default(autoincrement())

  type      ActivityType
  message   String?

  userId    Int
  user      User @relation(fields: [userId], references: [id])

  taskId    Int?
  task      Task? @relation(fields: [taskId], references: [id])

  createdAt DateTime @default(now())
}

enum ActivityType {
  TASK_CREATED
  TASK_UPDATED
  TASK_DONE
  PROJECT_CREATED
  MEMBER_ADDED
}








1. Продуктова ієрархія: 
Проєкти та Завдання (Task Management)Вище 
ми згадували, що команда не може існувати сама по собі. Мідл вміє 
будувати складні реляційні зв'язки.Що зробити: Додайте сутності 
Project (Один-до-багатьох з Team) та Task (Один-до-багатьох з Project).
Middle-фіча: 
Зробіть таски у вигляді найпростішої Канбан-дошки (колонки Todo, In Progress, Done) 
за допомогою бібліотеки @hello-pangea/dnd (колишній react-beautiful-dnd). 
Вміння реалізувати Drag-and-Drop інтерфейс, який синхронізується з базою 
даних через PATCH-запити, — це 100% мідл-задача.💳 

2. Симуляція білінгу 
та лімітів (SaaS Monetization)Головна відмінність SaaS від звичайного 
сайту — це обмеження тарифних планів (Free / Pro) [google]. Мідл розробник 
завжди думає про те, як бізнес зароблятиме гроші.Що зробити: Не 
обов'язково підключати реальний Stripe. Додайте в модель 
Team поле plan (enum: FREE, PRO).
Middle-фіча: 
Напишіть у NestJS інтерцептор 
(Interceptor) або Guard, який перед створенням нового проєкту чи додаванням 
користувача рахує поточну кількість у базі даних. Якщо на Free-плані адмін 
намагається створити 4-й проєкт (а ліміт — 3) [google], бекенд повертає 403 
PremiumRequired. На фронтенді покажіть красиве модальне вікно: "Оновіться до PRO, 
щоб зняти ліміти".

3. Асинхронний флоу: 
Запрошення замість прямого додаванняВи починали з MVP, 
де користувачі додаються миттєво. Настав час це вдосконалити, оскільки 
пряме додавання без згоди юзера — це діра в UX.Що зробити: Перепишіть додавання 
на систему інвайтів (як ми обговорювали на початку). Створіть таблицю 
Invitation (email, teamId, token, expiresAt).
Middle-фіча: 
Замість реальної 
відправки email (щоб не платити за сервіси), підключіть Nodemailer з безкоштовним 
сервісом Mailtrap. Коли адмін запрошує користувача, в консоль бекенду 
(або в кабінет Mailtrap) приходить реальний лист із посиланням 
http://localhost:3000/invite/token-123. Перехід за посиланням додає юзера в команду.

4. Логування активності 
(Audit Log / Activity History)У командних SaaS-проєктах критично важливо знати, 
хто що зробив. Це улюблена фіча Enterprise-клієнтів.Що зробити: Створіть 
таблицю ActivityLog (id, teamId, userId, action, createdAt).
Middle-фіча: 
Напишіть глобальний сервіс логування на бекенді. Коли хтось додає учасника, 
змінює назву команди чи видаляє таску, в цю таблицю записується рядок (наприклад: 
"Користувач Теmaphlow видалив таску #42"). На фронтенді зробіть окрему вкладку 
"Activity", де виведіть цей список, відформатований вашою функцією formatDate(date, true).

5. Оптимізація та безпека 
(Те, про що спитають на інтерв'ю)Мідл пише код, який не ляже, якщо в базу прийде 
10 000 користувачів.Пагінація (Pagination): Список тасок або логів не 
можна виводити одним масивом. Реалізуйте курсорну або офсетну пагінацію 
(take, skip в Prisma) [google].
Індекси в БД: 
У вашій schema.prisma додайте 
атрибути @@index([teamId]) для полів, за якими найчастіше йде пошук (where).
 Це покаже, що ви розумієте, як працюють бази даних під навантаженням.